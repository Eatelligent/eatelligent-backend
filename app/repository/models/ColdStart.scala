package repository.models

case class ColdStart(
                        id: Option[Long],
                        image: String,
                        identifier: String,
                        description: String
                        )
