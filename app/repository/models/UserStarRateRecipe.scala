package repository.models

case class UserStarRateRecipe(
                                 userId: String,
                                 recipeId: Long,
                                 stars: Double
                                 )
